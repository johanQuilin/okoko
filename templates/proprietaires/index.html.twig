{% extends 'base.html.twig' %}

{% block title %}Gestion des chatons en BDD{% endblock %}

{% block main %}
    <h1>Gestion des chatons en BDD</h1>
    <table class="table table-striped">
        <tr>
            <th>nom </th>
            <th>prenom</th>
            <th></th>
        </tr>
        {% for p in proprietaires %}
            <tr>
                <td>{{ p.nom }}</td>
                <td>{{ p.prenom }}</td>
                <td>
                    <

                </td>
            </tr>
        {% endfor %}
    </table>
    <a href="{{path("app_proprietaires_ajouter")}}" class="btn btn-success">Ajouter un proprietaires</a>

{% endblock %}
